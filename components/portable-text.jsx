'use client'
import { PortableText } from '@portabletext/react'

const PortableTextWrapper = ({ portableText }) => {
  return <PortableText value={portableText} />
}

export default PortableTextWrapper
